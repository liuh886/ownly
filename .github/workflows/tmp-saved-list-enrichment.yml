name: Apply Saved List Enrichment Fix

on:
  push:
    branches:
      - fix/capture-saved-list-enrichment

permissions:
  contents: write

jobs:
  patch-test-commit:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Apply focused patch
        run: python .github/tmp-saved-list-enrichment.py

      - name: Install dependencies
        run: npm ci

      - name: Validate repository
        run: npm run validate

      - name: Commit validated fix
        shell: bash
        run: |
          rm .github/tmp-saved-list-enrichment.py
          rm .github/workflows/tmp-saved-list-enrichment.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix(capture): make saved-list enrichment one-click and provider-authoritative"
          git push origin HEAD:fix/capture-saved-list-enrichment
