// PR1 identity layer — re-export from core (structure only, logic unchanged)
export * from '../google-maps-research';
export { featureIdToCid, googleMapsPreviewPlaceUrl, googleMapsSearchTbmUrl, extractFeatureIdFromHtml, googleMapsDetailUrlFromSourceId } from '../google-maps-research';
